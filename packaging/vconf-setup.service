[Unit]
Description=Initialize the VConf storage
DefaultDependencies=no
RequiresMountsFor=/var/run /opt
Before=basic.target

[Service]
Type=oneshot
ExecStart=/usr/bin/vconf-init

[Install]
WantedBy=basic.target
